# Example Data

This folder contains the provided CAN and GPS datasets used by the project.
They can be used directly to run the pipeline:

```bash
python run_pipeline.py --can examples/Task2_CAN_data.csv --gps examples/Task2_GPS_data.csv
```
